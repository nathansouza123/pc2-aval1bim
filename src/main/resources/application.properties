# EMBEDDED SERVER CONFIGURATION
server.port=8089
server.servlet.context-path=/avaliacao

# JPA
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.id.new_generator_mappings=false

# DATASOURCE
spring.datasource.url=jdbc:mysql://localhost:3306/PC2_AVALIACAO_1B?serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=123456
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# LOGGING
logging.level.*=INFO
logging.pattern.console=%d{dd/MM/yyyy HH:mm:ss} - %msg%n

# c3p0  pooling
hibernate.c3p0.max_size=20
hibernate.c3p0.min_size=5
hibernate.c3p0.idle_test_period=600
